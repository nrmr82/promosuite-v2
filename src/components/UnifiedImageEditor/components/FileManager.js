import React from 'react';

const FileManager = ({ store, onSave, onExport, currentFile }) => {
  // This is a placeholder - in a full implementation this would be a modal
  // that manages file operations
  return null;
};

export default FileManager;